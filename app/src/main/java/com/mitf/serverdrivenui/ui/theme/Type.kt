package com.mitf.serverdrivenui.ui.theme

import androidx.compose.material.Typography
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp

// Set of Material typography styles to start with
val Typography = Typography(
    body1 = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 16.sp
    ),
    caption = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 14.sp
    ),
    h1 = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 80.sp
    ),
    h2 = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 60.sp
    ),
    h3 = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 40.sp
    ),
    h4 = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 30.sp
    ),
    h5 = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 24.sp
    ),
    h6 = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 20.sp
    ),
    button = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 20.sp
    ),
)

/**
 * Typography for Heavy Text Style
 * Font Family Use KBFG Heavy
 * Font weight use 700 for Heavy Style
 */
val Heading1Heavy = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 80.sp
)

val Heading2Heavy = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 60.sp
)

val Heading3Heavy = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 40.sp
)

val Heading4Heavy = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 30.sp
)

val Heading5Heavy = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 24.sp
)

val Heading6Heavy = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 20.sp
)

val BodyHeavy = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 16.sp
)

val CaptionHeavy = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 14.sp
)

val SmallHeavy = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 12.sp
)

val TinyHeavy = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 10.sp
)

/**
 * Typography for Bold Text Style
 * Font Family Use KBFG Bold
 * Font weight use 700 for Bold Style
 */
val Heading1Bold = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 80.sp
)

val Heading2Bold = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 60.sp
)

val Heading3Bold = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 40.sp
)

val Heading4Bold = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 30.sp
)

val Heading5Bold = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 24.sp
)

val Heading6Bold = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 20.sp
)

val BodyBold = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 16.sp
)

val CaptionBold = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 14.sp
)

val SmallBold = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 12.sp
)

val TinyBold = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W700,
    fontSize = 10.sp
)

/**
 * Typography for Medium Text Style
 * Font Family Use KBFG Medium
 * Font weight use 500 for Medium Style
 */
val Heading1Medium = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 80.sp
)

val Heading2Medium = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 60.sp
)

val Heading3Medium = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 40.sp
)

val Heading4Medium = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 30.sp
)

val Heading5Medium = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 24.sp
)

val Heading6Medium = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 20.sp
)

val BodyMedium = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 16.sp
)

val CaptionMedium = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 14.sp
)

val SmallMedium = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 12.sp
)

val TinyMedium = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 10.sp
)

/**
 * Typography for Regular Text Style
 * Font Family Use KBFG Regular
 * Font weight use 500 for Regular Style
 */
val Heading1Regular = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 80.sp
)

val Heading2Regular = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 60.sp
)

val Heading3Regular = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 40.sp
)

val Heading4Regular = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 30.sp
)

val Heading5Regular = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 24.sp
)

val Heading6Regular = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 20.sp
)

val BodyRegular = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 16.sp
)

val CaptionRegular = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 14.sp
)

val SmallRegular = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 12.sp
)

val TinyRegular = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 10.sp
)

/**
 * Typography for Thin Text Style
 * Font Family Use KBFG Thin
 * Font weight use 300 for Thin Style
 */
val Heading1Thin = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W300,
    fontSize = 80.sp
)

val Heading2Thin = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W300,
    fontSize = 60.sp
)

val Heading3Thin = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W300,
    fontSize = 40.sp
)

val Heading4Thin = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W300,
    fontSize = 30.sp
)

val Heading5Thin = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W300,
    fontSize = 24.sp
)

val Heading6Thin = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W300,
    fontSize = 20.sp
)

val BodyThin = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W300,
    fontSize = 16.sp
)

val CaptionThin = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W300,
    fontSize = 14.sp
)

val SmallThin = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W300,
    fontSize = 12.sp
)

val TinyThin = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W300,
    fontSize = 10.sp
)

/**
 * Typography for Button Text Style
 * Font Family Use KBFG Default
 * Font weight use 500 for Button Style
 */
val ButtonXLarge = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 23.sp
)

val ButtonLarge = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 18.sp
)

val ButtonMedium = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 14.sp
)

val ButtonSmall = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 12.sp
)

/**
 * Typography for List Text Style
 * Font Family Use KBFG
 * Font weight use 500 for List Style
 */
val ListTypography = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.W500,
    fontSize = 14.sp
)
